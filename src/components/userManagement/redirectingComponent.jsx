/*
import {useState} from "react";
import {useHistory} from "react-router-dom";

const RedirectingComponent = ({currentLocation}) => {
    // Variables
    const[message, setMessage] = useState('');

    switch(currentLocation)
    {
        case('login'): setMessage('Willkommen,');
        break;
        case('logout'): setMessage('Auf Wiedersehen,');
        break;
        case('signup'): setMessage('Willkommen,');
        break;
        default: setMessage('...');
        break;
    }
    return(
        <div>
            <h2>{message}</h2>
            sie werden weitergeleitet.
        </div>

    )
}
export default RedirectingComponent;*/

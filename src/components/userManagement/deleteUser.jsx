/*
export default const DeleteUser = ({id}) => {
    return (
        fetch('http://localhost:5000/users/'+id, {
            method: 'DELETE'
    }).then(() => {
        console.log('User successfully deleted');
        })
    )
}

*/
